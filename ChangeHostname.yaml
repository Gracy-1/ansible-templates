- name: Change Hostname of Remote Machine
  hosts: all  # Replace with the group name from your Semaphore inventory
  become: true  # This allows running tasks with root privileges
  tasks:
    - name: Update /etc/hostname
      lineinfile:
        path: /etc/hostname
        regexp: '^.*$'
        line: 'IND-STL-ANALYTICS-APP4'  # Replace with the new hostname you want to set
      notify:
        - Restart Hostname Service

  handlers:
    - name: Restart Hostname Service
      systemd:
        name: systemd-hostnamed
        state: restarted
